parameters:
  - name: gradle_task
    type: string
    default: "check assemble$(flavor)"
  - name: unit_tests
    type: boolean
    default: false

steps:
  - task: Gradle@2
    inputs:
      workingDirectory: ''
      gradleWrapperFile: 'gradlew'
      gradleOptions: '-Xmx3072m'
      publishJUnitResults: ${{ parameters.unit_tests }}
      testResultsFiles: '**/TEST-*.xml'
      tasks: '${{ parameters.gradle_task }} -Papp_secret=$(app_secret) -PversionCode=$(Build.BuildNumber)'